# Nginx 反向代理：按域名分流，访问时无需加端口（80）
# 构建上下文为项目根目录：docker build -f deploy/Dockerfile.proxy -t mop-proxy .
FROM nginx:alpine
RUN rm -f /etc/nginx/conf.d/default.conf
COPY deploy/nginx/conf.d/mop-proxy.conf /etc/nginx/conf.d/mop-proxy.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
